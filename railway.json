{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd backend && python -c 'from database import init_db; init_db()' && uvicorn app:app --host 0.0.0.0 --port $PORT",
        "restartPolicyType": "ON_FAILURE"
    }
}
